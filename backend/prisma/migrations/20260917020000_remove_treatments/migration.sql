-- Matt: Blue Duck already documents treatment in its operational software;
-- duplicating that inside the inspection app serves no purpose. Removes the
-- Treatment section entirely (child table first).
DROP TABLE "TreatmentProduct";

DROP TABLE "TreatmentRecord";
